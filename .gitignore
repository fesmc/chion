# Generated by configme. Anchored to the repo root: config/Makefile is the
# TEMPLATE and must stay tracked. An unanchored "Makefile" pattern matches at
# every level and silently excludes it.
/Makefile

# Build products. The precision=dp build gets its own suffixed directories so
# it can coexist with the sp build (config/Makefile: psuffix).
libchion/include/*
libchion/bin/*
libchion/include-dp/
libchion/bin-dp/
!libchion/include/.gitkeep
!libchion/bin/.gitkeep
*.o
*.mod
*.a
*.x

# Dependency slots (symlinked or cloned by configme)
fesm-utils

# Build provenance
git_chion.txt

# Run output
output/
logs/
tmp/

# runme per-user config
.runme/config.toml

# macOS
.DS_Store

# WP16 harness run products
validation/work/
